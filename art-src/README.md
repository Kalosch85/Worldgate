# art-src

Source folder for art assets (sprites, tilesets, source images, and other raw
art files) used by Worldgate.

This directory holds **source** art — the editable originals. Built or exported
assets consumed by the app belong under `src/` (or the build output), not here.
